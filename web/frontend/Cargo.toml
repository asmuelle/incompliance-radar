[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
app = { path = "../app" }
leptos = { version = "0.8", features = ["hydrate"] }
console_error_panic_hook = "0.1"
wasm-bindgen = "0.2"
